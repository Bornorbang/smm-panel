"use client";import { FormEvent,useEffect,useState } from "react";import { api,User } from "@/lib/api";
export default function Account(){const[user,setUser]=useState<User|null>(null);const[message,setMessage]=useState("");const[error,setError]=useState("");useEffect(()=>{api<{user:User}>("/api/auth/me").then(r=>setUser(r.user)).catch(e=>setError(e.message))},[]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();setMessage("");setError("");const body=Object.fromEntries(new FormData(e.currentTarget));try{const result=await api<{user:User}>("/api/auth/me",{method:"PATCH",body:JSON.stringify(body)});setUser(result.user);setMessage("Profile updated successfully.")}catch(reason){setError(reason instanceof Error?reason.message:"Update failed.")}}return <><div className="dash-heading"><div><span className="dash-kicker">Preferences</span><h1>Account settings</h1><p>Manage your profile and integration readiness.</p></div></div><div className="account-grid"><form className="dash-panel" onSubmit={save}><div className="panel-head"><div><h2>Profile information</h2><p>Used for your workspace identity.</p></div></div>{message&&<div className="dash-alert success">{message}</div>}{error&&<div className="dash-alert error">{error}</div>}{user?<><div className="field"><label htmlFor="name">Full name</label><input id="name" name="name" defaultValue={user.name} required minLength={2}/></div><div className="field"><label htmlFor="email">Email address</label><input id="email" name="email" defaultValue={user.email} required type="email"/></div><div className="field"><label>Member since</label><input value={new Date(user.created_at).toLocaleDateString()} disabled/></div><button className="button" type="submit">Save changes</button></>:<div className="table-loading">Loading profile...</div>}</form><aside className="dash-panel security-card"><div className="panel-head"><div><h2>Security</h2><p>Your account protection.</p></div></div><div className="setting-row"><span>⌾</span><div><strong>Password</strong><small>Stored with secure one-way hashing</small></div><button disabled>Change soon</button></div><div className="setting-row"><span>◈</span><div><strong>Provider credentials</strong><small>Kept securely on the PHP server</small></div><em>Protected</em></div><div className="setting-row"><span>◉</span><div><strong>Session</strong><small>HTTP-only cookie authentication</small></div><em>Active</em></div></aside></div></>}
