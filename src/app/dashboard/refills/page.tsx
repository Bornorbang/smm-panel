"use client";
import { useEffect,useState } from "react";import { api,Refill } from "@/lib/api";import { StatusPill } from "@/components/status-pill";
export default function Refills(){const[items,setItems]=useState<Refill[]>([]);const[error,setError]=useState("");useEffect(()=>{api<{refills:Refill[]}>("/api/refills").then(r=>setItems(r.refills)).catch(e=>setError(e.message))},[]);return <><div className="dash-heading"><div><span className="dash-kicker">Order support</span><h1>Refill requests</h1><p>Monitor refill requests for eligible orders.</p></div></div><div className="dash-panel">{error?<div className="dash-alert error">{error}</div>:!items.length?<div className="dash-empty"><span>↻</span><h3>No refill requests</h3><p>Eligible requests created from Orders will appear here.</p></div>:<div className="dash-table-wrap"><table className="dash-table"><thead><tr><th>Refill</th><th>Order</th><th>Service</th><th>Requested</th><th>Status</th></tr></thead><tbody>{items.map(r=><tr key={r.id}><td><strong>#{r.id}</strong></td><td>#{r.order_id}</td><td><strong>{r.service_name}</strong></td><td>{new Date(r.created_at).toLocaleDateString()}</td><td><StatusPill status={r.status}/></td></tr>)}</tbody></table></div>}</div></>}
